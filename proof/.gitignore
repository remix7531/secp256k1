# clightgen-generated Clight ASTs
clight/*.v

# rocq makefile + build artifacts
Makefile.rocq
Makefile.rocq.conf
.Makefile.coq.d
*.vo
*.vok
*.vos
*.glob
*.aux
.*.aux
.lia.cache
.nia.cache
.Makefile.rocq.d
.axioms/

# timing data (TIMED=1 / TIMING=1 defaults, make pretty-timed)
*.v.timing
*.v.before-timing
*.v.after-timing
*.v.timing.diff
time-of-build*.log
pretty-timed-success.ok

# coqdoc rendering of the story files (make html)
html/
